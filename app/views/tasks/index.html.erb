<h1>My tasks</h1>
  <% @tasks.each do |task| %>
    <p><%= check_box_tag "checked[#{task.id}]","#{task.id}", task.completed %>
    <strong> <%= link_to task.title.upcase, task_path(task.id) %></strong></p>
  <% end %>

 <%= link_to "+ Add a task", tasks_new_path %>

